# 404 错误修复说明

## 修复的问题

之前访问 http://localhost:3001 时控制台出现的 404 错误：
1. ❌ `favicon.ico` - 404 Not Found
2. ❌ `/.output/` - 404 Not Found

## 解决方案

### 1. Favicon 支持

#### HTML 添加 favicon 链接
```html
<link rel="icon" type="image/png" href="icon/128.png">
```

#### 服务器支持 favicon 请求
在 `dev-server-unified.js` 中添加：

```javascript
// 处理 favicon
if (req.url === '/favicon.ico') {
  if (existsSync(FAVICON_FILE)) {
    readFile(FAVICON_FILE).then(data => {
      res.writeHead(200, { 'Content-Type': 'image/png' });
      res.end(data);
    });
  }
  return;
}

// 处理 icon 图片请求
if (req.url.startsWith('/icon/')) {
  const iconPath = join(__dirname, 'public', req.url);
  if (existsSync(iconPath)) {
    readFile(iconPath).then(data => {
      res.writeHead(200, { 'Content-Type': 'image/png' });
      res.end(data);
    });
  }
  return;
}
```

### 2. 简化扩展下载逻辑

移除了尝试访问 `/.output/` 的逻辑，改为直接显示安装指南：

```javascript
window.downloadExtension = function() {
  const message = `📦 SideDoor 扩展安装指南

开发版安装（推荐）：
1. 确保已运行 npm run build 构建扩展
2. 打开 Chrome 浏览器，访问 chrome://extensions/
3. 开启右上角的"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择项目目录下的 .output/chrome-mv3 文件夹

线上安装：
• 访问 Chrome Web Store 搜索 "SideDoor"
• 或访问项目 GitHub 页面获取最新版本

安装完成后刷新此页面即可使用完整功能！`;
  
  alert(message);
};
```

## 修改的文件

1. **public/read-later-standalone.html**
   - 添加 `<link rel="icon">` 标签
   - 简化 `downloadExtension()` 函数

2. **dev-server-unified.js**
   - 添加 favicon 处理
   - 添加 `/icon/` 路径处理

## 结果

✅ 不再有 404 错误  
✅ 浏览器标签页显示正确的 favicon  
✅ 扩展安装指南更清晰  

## 测试

访问 http://localhost:3001 后，控制台应该没有任何 404 错误。
