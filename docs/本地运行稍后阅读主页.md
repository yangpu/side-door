# 💻 本地运行稍后阅读主页

快速启动本地 Web 服务，访问你的稍后阅读列表。

---

## 🚀 快速开始

### 一键启动

```bash
npm run serve:read-later
```

### 访问

浏览器打开：**http://localhost:3001**

---

## 📋 详细说明

### 服务特性

- ✅ 提供 HTML 页面服务
- ✅ 支持 CORS（跨域访问）
- ✅ 正确的 Content-Type (`text/html`)
- ✅ 自动显示局域网 IP（手机可访问）
- ✅ 健康检查端点
- ✅ 请求日志记录

### 端口配置

默认端口：**3001**

修改端口：
```bash
PORT=8080 npm run serve:read-later
```

---

## 🌐 局域网访问

启动服务后，会显示局域网 IP：

```
🌐 局域网访问:
   http://192.168.1.100:3001
```

用手机或其他设备访问这个地址，即可在同一网络下查看。

---

## 🔧 高级用法

### 同时运行开发服务器和稍后阅读服务

```bash
# 终端 1：启动扩展开发服务器
npm run dev

# 终端 2：启动稍后阅读服务
npm run serve:read-later
```

### 健康检查

```bash
curl http://localhost:3001/health
```

返回：
```json
{
  "status": "ok",
  "service": "read-later-home",
  "timestamp": "2025-11-26T15:30:00.000Z"
}
```

### 查看请求日志

服务会自动记录所有请求：

```
[2025-11-26T15:30:00.000Z] GET / - Mozilla/5.0 ...
[2025-11-26T15:30:05.000Z] GET / - Mozilla/5.0 ...
```

---

## 🛑 停止服务

在运行服务的终端按 **Ctrl+C**

---

## 📝 文件说明

### serve-read-later.js

本地 HTTP 服务器脚本，功能包括：

- 读取 `public/read-later-standalone.html`
- 提供正确的 Content-Type
- 支持 CORS
- 健康检查端点
- 优雅关闭

### public/read-later-standalone.html

稍后阅读主页的 HTML 文件，包含：

- 完整的页面结构
- 内联的 CSS 样式
- JavaScript 功能
- Supabase 集成

---

## 🔄 开发工作流

1. **启动服务**
   ```bash
   npm run serve:read-later
   ```

2. **编辑 HTML 文件**
   - 修改 `public/read-later-standalone.html`

3. **刷新浏览器**
   - 直接刷新即可看到更新
   - 无需重启服务（HTML 每次请求都会重新读取）

4. **修改服务器代码**
   - 如果修改了 `serve-read-later.js`
   - 需要 Ctrl+C 停止服务
   - 重新运行 `npm run serve:read-later`

---

## 🐛 故障排除

### 端口已被占用

**错误**：`Error: listen EADDRINUSE: address already in use :::3001`

**解决**：
```bash
# 查找占用端口的进程
lsof -i :3001

# 杀死进程
kill -9 <PID>

# 或使用其他端口
PORT=3002 npm run serve:read-later
```

### 找不到 HTML 文件

**错误**：`❌ 错误: 找不到文件 ...`

**解决**：确保在项目根目录运行命令，且 `public/read-later-standalone.html` 存在。

### 无法访问

1. 检查防火墙
2. 确保服务正在运行
3. 使用 `curl http://localhost:3001` 测试

---

## 🎯 后续步骤

本地测试完成后，可以：

1. **部署到云服务器**
   - 参考：`docs/部署到云服务器.md`
   - 使用 Nginx 提供服务

2. **集成到开发工作流**
   - 添加到启动脚本
   - 配合浏览器扩展使用

3. **自定义功能**
   - 修改 `serve-read-later.js` 添加自定义逻辑
   - 添加 API 端点

---

## 📚 相关文档

- [部署到云服务器](./部署到云服务器.md)
- [nginx.conf.example](../nginx.conf.example) - Nginx 配置示例
- [serve-read-later.js](../serve-read-later.js) - 服务器脚本

---

**现在就试试吧！** 🚀

```bash
npm run serve:read-later
```

然后打开 http://localhost:3001
